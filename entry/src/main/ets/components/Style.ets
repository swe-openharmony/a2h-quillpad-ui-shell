// Shared Material-light design tokens for the Quillpad shell, matched to the
// Android captures (white app background, blue accent #2196F3, dark-on-light text).

export class Style {
  static readonly accent: string = '#2196F3';
  static readonly bg: string = '#FFFFFF';
  static readonly textPrimary: string = '#1A1A1A';
  static readonly textSecondary: string = '#5F6368';
  static readonly textHint: string = '#9AA0A6';
  static readonly divider: string = '#E0E0E0';
  static readonly icon: string = '#5F6368';
  static readonly fabBlue: string = '#1565C0';
  static readonly selectionBg: string = '#D6E8FB';
  static readonly chipBorder: string = '#BDBDBD';

  static readonly toolbarHeight: number = 56;
  static readonly titleSize: number = 22;
  static readonly bodySize: number = 16;
  static readonly iconSize: number = 24;
}
